// server/simplification.js

/**
 * This function is a placeholder. It originally simplified text using an AI model,
 * but that functionality has been disabled. It now returns the original text.
 * @param {string} text - The text to simplify.
 * @returns {Promise<string>} - The original, unmodified text.
 */
async function simplifyText(text) {
  // Simplification has been disabled. Return the original text directly.
  return text;
}

module.exports = { simplifyText };
